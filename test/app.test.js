const assert = require("chai").assert;

describe('App', () => {
  it('Should fix the travis error', () => {
    assert.equal(2, 2);
  })
})
describe('App', () => {
  it('Additional Passing test case', () => {
    assert.equal(8, 8);
  })
})